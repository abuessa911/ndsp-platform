# NDSP Core Services Gateway Readiness Example
# DO NOT COPY AUTOMATICALLY.
# Documentation only until an explicit gateway deployment task.

# location = /api/completed/latest {
#   proxy_pass http://127.0.0.1:9078/api/completed/latest;
#   proxy_set_header Host $host;
#   proxy_set_header X-Real-IP $remote_addr;
#   proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
#   proxy_set_header X-Forwarded-Proto $scheme;
# }
#
# location = /api/governance/health {
#   proxy_pass http://127.0.0.1:9079/health;
#   proxy_set_header Host $host;
#   proxy_set_header X-Real-IP $remote_addr;
#   proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
#   proxy_set_header X-Forwarded-Proto $scheme;
# }

# Explicitly keep BOT-001 private.
# Explicitly keep CTL-001 private.
# Explicitly block write paths unless future signed internal gateway is created.
