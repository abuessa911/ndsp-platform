=== NDSP HOME DASHBOARD PRECHECK V1 ===
MODE=READ_ONLY
DATE=2026-08-08T06:07:54+02:00
PROJECT=/home/nawaf511/empire-core-new/frontend/user-portal-vite
AUDIT=/home/nawaf511/empire-core-new/var/audits/home-dashboard-precheck-v1-20260808-060754

=== 1. SHELL FILES ===
/home/nawaf511/empire-core-new/frontend/user-portal-vite/src/components/layout/ApplicationShell.tsx
/home/nawaf511/empire-core-new/frontend/user-portal-vite/src/components/layout/MobileNavigation.tsx
/home/nawaf511/empire-core-new/frontend/user-portal-vite/src/components/layout/Sidebar.tsx
/home/nawaf511/empire-core-new/frontend/user-portal-vite/src/components/layout/Topbar.tsx
/home/nawaf511/empire-core-new/frontend/user-portal-vite/src/config/navigation.ts

=== 2. APPLICATION SHELL SOURCE ===
APPLICATION_SHELL=/home/nawaf511/empire-core-new/frontend/user-portal-vite/src/components/layout/ApplicationShell.tsx
     1	import {
     2	  type PropsWithChildren,
     3	  useEffect,
     4	  useState
     5	} from "react";
     6	
     7	import { MobileNavigation } from "./MobileNavigation";
     8	import { Sidebar } from "./Sidebar";
     9	import { Topbar } from "./Topbar";
    10	
    11	type Props = PropsWithChildren<{
    12	  pathname?: string;
    13	}>;
    14	
    15	export function ApplicationShell({
    16	  pathname = window.location.pathname,
    17	  children
    18	}: Props) {
    19	  const [mobileOpen, setMobileOpen] =
    20	    useState(false);
    21	
    22	  useEffect(() => {
    23	    setMobileOpen(false);
    24	  }, [pathname]);
    25	
    26	  useEffect(() => {
    27	    document.body.classList.add(
    28	      "ndsp-application-shell-enabled"
    29	    );
    30	
    31	    return () => {
    32	      document.body.classList.remove(
    33	        "ndsp-application-shell-enabled"
    34	      );
    35	    };
    36	  }, []);
    37	
    38	  return (
    39	    <div className="ndsp-app-shell">
    40	      <Sidebar
    41	        pathname={pathname}
    42	        mobileOpen={mobileOpen}
    43	        onMobileClose={() => setMobileOpen(false)}
    44	      />
    45	
    46	      <div className="ndsp-app-shell__workspace">
    47	        <Topbar
    48	          onMenuOpen={() => setMobileOpen(true)}
    49	        />
    50	
    51	        <main className="ndsp-app-shell__main">
    52	          {children}
    53	        </main>
    54	      </div>
    55	
    56	      <MobileNavigation pathname={pathname} />
    57	    </div>
    58	  );
    59	}

=== 3. NAVIGATION REGISTRY ===
/home/nawaf511/empire-core-new/frontend/user-portal-vite/src/config/navigation.ts:50:    id: "completed",
/home/nawaf511/empire-core-new/frontend/user-portal-vite/src/config/navigation.ts:53:    href: "/portal/completed",
/home/nawaf511/empire-core-new/frontend/user-portal-vite/src/config/navigation.ts:60:    id: "portfolio",
/home/nawaf511/empire-core-new/frontend/user-portal-vite/src/hooks/useCompletedDecisionsV37.js:4:  current: "/api/completed-decisions",
/home/nawaf511/empire-core-new/frontend/user-portal-vite/src/hooks/useCompletedDecisionsV37.js:5:  history: "/api/completed-decisions/history?limit=100",
/home/nawaf511/empire-core-new/frontend/user-portal-vite/src/hooks/useCompletedDecisionsV37.js:6:  integrity: "/api/completed-decisions/history/integrity"
/home/nawaf511/empire-core-new/frontend/user-portal-vite/src/components/CompletedDecisionsPanelV37.jsx:127:    noCurrent: "No completed decision matches the current filters.",
/home/nawaf511/empire-core-new/frontend/user-portal-vite/src/components/CompletedDecisionsPanelV37.jsx:166:    investmentBlockedText: "The investment reading is not a completed decision, so it is not shown in the completed-decision registry.",
/home/nawaf511/empire-core-new/frontend/user-portal-vite/src/components/CompletedDecisionsPanelV37.jsx:167:    investmentReadyText: "The investment reading passed readiness gates and will appear after it is published as a completed decision.",
/home/nawaf511/empire-core-new/frontend/user-portal-vite/src/components/CompletedDecisionsPanelV37.jsx:507:      id="completed-decisions"
/home/nawaf511/empire-core-new/frontend/user-portal-vite/src/components/CompletedDecisionsPanelV37.jsx:508:      className="card completed v37Completed"
/home/nawaf511/empire-core-new/frontend/user-portal-vite/src/components/CompletedDecisionsPanelV37.jsx:509:      data-ndsp-v37="completed-decisions"
/home/nawaf511/empire-core-new/frontend/user-portal-vite/src/components/CompletedDecisionsPanelV37.jsx:566:        <p>{investmentExecution?.publishable_completed_decision ? tr.investmentReadyText : tr.investmentBlockedText}</p>
/home/nawaf511/empire-core-new/frontend/user-portal-vite/src/main.jsx:64:  const completed = livePrice ? 7 : 0;

=== 4. UI PRIMITIVES ===
--- FILE=/home/nawaf511/empire-core-new/frontend/user-portal-vite/src/components/ui/Badge.tsx ---
import type { PropsWithChildren } from "react";
import type { Tone } from "../../types/ui";

type Props = PropsWithChildren<{
  tone?: Tone;
}>;

export function Badge({
  tone = "neutral",
  children
}: Props) {
  return (
    <span className={`ndsp-badge ndsp-badge--${tone}`}>
      {children}
    </span>
  );
}
--- FILE=/home/nawaf511/empire-core-new/frontend/user-portal-vite/src/components/ui/Button.tsx ---
import type {
  ButtonHTMLAttributes,
  PropsWithChildren
} from "react";

import { cn } from "../../lib/cn";

type Variant = "primary" | "secondary" | "ghost" | "danger";

type Props = PropsWithChildren<
  ButtonHTMLAttributes<HTMLButtonElement> & {
    variant?: Variant;
  }
>;

export function Button({
  children,
  variant = "primary",
  className,
  ...props
}: Props) {
  return (
    <button
      className={cn(
        "ndsp-button",
        `ndsp-button--${variant}`,
        className
      )}
      {...props}
    >
      {children}
    </button>
  );
}
--- FILE=/home/nawaf511/empire-core-new/frontend/user-portal-vite/src/components/ui/Card.tsx ---
import type { PropsWithChildren } from "react";

type Props = PropsWithChildren<{
  title?: string;
  subtitle?: string;
}>;

export function Card({
  title,
  subtitle,
  children
}: Props) {
  return (
    <section className="ndsp-card ndsp-ui-card">
      {(title || subtitle) && (
        <header className="ndsp-ui-card__header">
          {title && <h2>{title}</h2>}
          {subtitle && <p>{subtitle}</p>}
        </header>
      )}

      <div className="ndsp-ui-card__body">
        {children}
      </div>
    </section>
  );
}

=== 5. FOUNDATION CONTRACT ===
--- FILE=/home/nawaf511/empire-core-new/frontend/user-portal-vite/src/config/uiFoundation.ts ---
export const uiFoundation = Object.freeze({
  version: "1.0.0",
  canonicalBase: "/portal/",
  defaultLanguage: "ar",
  supportedLanguages: ["ar", "en"],
  direction: {
    ar: "rtl",
    en: "ltr"
  },
  productionDeployEnabled: false
});
--- FILE=/home/nawaf511/empire-core-new/frontend/user-portal-vite/src/styles/tokens.css ---
:root {
  --ndsp-bg: #07111f;
  --ndsp-surface: #0d1929;
  --ndsp-surface-2: #122237;
  --ndsp-surface-3: #182b43;

  --ndsp-border: rgba(148, 163, 184, 0.16);
  --ndsp-border-strong: rgba(148, 163, 184, 0.28);

  --ndsp-text: #f8fafc;
  --ndsp-text-secondary: #cbd5e1;
  --ndsp-text-muted: #94a3b8;

  --ndsp-primary: #38bdf8;
  --ndsp-primary-strong: #0ea5e9;
  --ndsp-primary-soft: rgba(56, 189, 248, 0.12);

  --ndsp-success: #34d399;
  --ndsp-warning: #fbbf24;
  --ndsp-danger: #fb7185;
  --ndsp-info: #60a5fa;

  --ndsp-radius-sm: 8px;
  --ndsp-radius-md: 12px;
  --ndsp-radius-lg: 16px;
  --ndsp-radius-xl: 22px;

  --ndsp-space-1: 4px;
  --ndsp-space-2: 8px;
  --ndsp-space-3: 12px;
  --ndsp-space-4: 16px;
  --ndsp-space-5: 20px;
  --ndsp-space-6: 24px;
  --ndsp-space-8: 32px;

  --ndsp-shadow:
    0 18px 45px rgba(0, 0, 0, 0.22);

  --ndsp-content-max: 1440px;
  --ndsp-sidebar-width: 272px;

  font-family:
    Inter,
    "IBM Plex Sans Arabic",
    "Noto Sans Arabic",
    system-ui,
    -apple-system,
    BlinkMacSystemFont,
    "Segoe UI",
    sans-serif;

  color-scheme: dark;
}
--- FILE=/home/nawaf511/empire-core-new/frontend/user-portal-vite/src/styles/foundation.css ---
*,
*::before,
*::after {
  box-sizing: border-box;
}

html {
  min-width: 320px;
  min-height: 100%;
  background: var(--ndsp-bg);
}

body {
  margin: 0;
  min-width: 320px;
  min-height: 100vh;
  background:
    radial-gradient(
      circle at top,
      rgba(14, 165, 233, 0.08),
      transparent 34rem
    ),
    var(--ndsp-bg);
  color: var(--ndsp-text);
}

button,
input,
select,
textarea {
  font: inherit;
}

button,
a {
  -webkit-tap-highlight-color: transparent;
}

a {
  color: inherit;
}

#root {
  min-height: 100vh;
}

.ndsp-container {
  width: min(
    calc(100% - 32px),
    var(--ndsp-content-max)
  );
  margin-inline: auto;
}

.ndsp-card {
  border: 1px solid var(--ndsp-border);
  border-radius: var(--ndsp-radius-lg);
  background:
    linear-gradient(
      180deg,
      rgba(255,255,255,0.025),
      transparent
    ),
    var(--ndsp-surface);
  box-shadow: var(--ndsp-shadow);
}

.ndsp-stack {
  display: flex;
  flex-direction: column;
  gap: var(--ndsp-space-4);
}

.ndsp-row {
  display: flex;
  align-items: center;
  gap: var(--ndsp-space-3);
}

.ndsp-muted {
  color: var(--ndsp-text-muted);
}

.ndsp-page {
  width: 100%;
  padding-block: var(--ndsp-space-6);
}

@media (max-width: 760px) {
  .ndsp-container {
    width: min(calc(100% - 20px), var(--ndsp-content-max));
  }

  .ndsp-page {
    padding-block: var(--ndsp-space-4);
  }
}
--- FILE=/home/nawaf511/empire-core-new/frontend/user-portal-vite/src/styles/components.css ---
.ndsp-button {
  min-height: 42px;
  border: 1px solid transparent;
  border-radius: var(--ndsp-radius-md);
  padding: 0 16px;
  cursor: pointer;
  transition:
    background-color 140ms ease,
    border-color 140ms ease,
    transform 140ms ease;
}

.ndsp-button:active {
  transform: translateY(1px);
}

.ndsp-button:focus-visible {
  outline: 2px solid var(--ndsp-primary);
  outline-offset: 2px;
}

.ndsp-button--primary {
  background: var(--ndsp-primary);
  color: #03111c;
}

.ndsp-button--secondary {
  border-color: var(--ndsp-border-strong);
  background: var(--ndsp-surface-2);
  color: var(--ndsp-text);
}

.ndsp-button--ghost {
  background: transparent;
  color: var(--ndsp-text-secondary);
}

.ndsp-button--danger {
  background: var(--ndsp-danger);
  color: #21060c;
}

.ndsp-ui-card {
  overflow: hidden;
}

.ndsp-ui-card__header {
  padding: 18px 20px 0;
}

.ndsp-ui-card__header h2 {
  margin: 0;
  font-size: 1rem;
}

.ndsp-ui-card__header p {
  margin: 6px 0 0;
  color: var(--ndsp-text-muted);
  font-size: 0.875rem;
}

.ndsp-ui-card__body {
  padding: 20px;
}

.ndsp-badge {
  display: inline-flex;
  align-items: center;
  min-height: 26px;
  padding: 3px 9px;
  border: 1px solid var(--ndsp-border);
  border-radius: 999px;
  background: var(--ndsp-surface-2);
  color: var(--ndsp-text-secondary);
  font-size: 0.75rem;
  font-weight: 650;
}

.ndsp-badge--info {
  color: var(--ndsp-info);
}

.ndsp-badge--success {
  color: var(--ndsp-success);
}

.ndsp-badge--warning {
  color: var(--ndsp-warning);
}

.ndsp-badge--danger {
  color: var(--ndsp-danger);
}
--- FILE=/home/nawaf511/empire-core-new/frontend/user-portal-vite/src/styles/shell.css ---
:root {
  --ndsp-topbar-height: 72px;
  --ndsp-mobile-nav-height: 68px;
}

.ndsp-app-shell {
  min-height: 100vh;
  background: var(--ndsp-bg);
}

.ndsp-app-shell__workspace {
  min-height: 100vh;
  margin-inline-start: var(--ndsp-sidebar-width);
}

.ndsp-app-shell__main {
  min-height: calc(100vh - var(--ndsp-topbar-height));
  padding: 24px;
}

.ndsp-sidebar {
  position: fixed;
  inset-block: 0;
  inset-inline-start: 0;
  z-index: 50;
  display: flex;
  width: var(--ndsp-sidebar-width);
  flex-direction: column;
  border-inline-end: 1px solid var(--ndsp-border);
  background:
    linear-gradient(
      180deg,
      rgba(56, 189, 248, 0.035),
      transparent 240px
    ),
    #091524;
}

.ndsp-sidebar__header {
  display: flex;
  min-height: var(--ndsp-topbar-height);
  align-items: center;
  justify-content: space-between;
  padding: 0 18px;
  border-block-end: 1px solid var(--ndsp-border);
}

.ndsp-brand {
  display: flex;
  align-items: center;
  gap: 11px;
}

.ndsp-brand__mark {
  display: grid;
  width: 38px;
  height: 38px;
  place-items: center;
  border: 1px solid rgba(56,189,248,.35);
  border-radius: 11px;
  background: var(--ndsp-primary-soft);
  color: var(--ndsp-primary);
  font-weight: 800;
  letter-spacing: -.04em;
}

.ndsp-brand__copy {
  display: flex;
  flex-direction: column;
  gap: 2px;
}

.ndsp-brand__copy strong {
  font-size: .92rem;
  letter-spacing: .08em;
}

.ndsp-brand__copy span {
  color: var(--ndsp-text-muted);
  font-size: .68rem;
}

.ndsp-sidebar__nav {
  display: flex;
  min-height: 0;
  flex: 1;
  flex-direction: column;
  overflow-y: auto;
  padding: 16px 12px;
}

.ndsp-nav-group {
  display: flex;
  flex-direction: column;
  gap: 5px;
}

.ndsp-nav-group--secondary {
  margin-top: auto;
}

.ndsp-nav-item {
  display: flex;
  min-height: 44px;
  align-items: center;
  gap: 11px;
  padding: 0 12px;
  border: 1px solid transparent;
  border-radius: 11px;
  color: var(--ndsp-text-muted);
  text-decoration: none;
  transition:
    background-color 140ms ease,
    color 140ms ease,
    border-color 140ms ease;
}

.ndsp-nav-item:hover {
  background: rgba(255,255,255,.035);
  color: var(--ndsp-text);
}

.ndsp-nav-item--active {
  border-color: rgba(56,189,248,.15);
  background: var(--ndsp-primary-soft);
  color: var(--ndsp-primary);
}

.ndsp-sidebar__separator {
  height: 1px;
  margin: 16px 8px;
  background: var(--ndsp-border);
}

.ndsp-sidebar__footer {
  display: flex;
  align-items: center;
  gap: 10px;
  padding: 15px 18px 18px;
  border-block-start: 1px solid var(--ndsp-border);
}

.ndsp-sidebar__footer > div {
  display: flex;
  min-width: 0;
  flex-direction: column;
  gap: 2px;
}

.ndsp-sidebar__footer strong {
  font-size: .75rem;
}

.ndsp-sidebar__footer span {
  overflow: hidden;
  color: var(--ndsp-text-muted);
  font-size: .68rem;
  text-overflow: ellipsis;
  white-space: nowrap;
}

.ndsp-status-dot {
  width: 8px;
  height: 8px;
  flex: 0 0 auto;
  border-radius: 50%;
  background: var(--ndsp-success);
  box-shadow: 0 0 0 4px rgba(52,211,153,.1);
}

.ndsp-topbar {
  position: sticky;
  inset-block-start: 0;
  z-index: 40;
  display: flex;
  min-height: var(--ndsp-topbar-height);
  align-items: center;
  justify-content: space-between;
  gap: 18px;
  padding: 0 24px;
  border-block-end: 1px solid var(--ndsp-border);
  background: rgba(7,17,31,.88);
  backdrop-filter: blur(18px);
}

.ndsp-topbar__start,
.ndsp-topbar__actions {
  display: flex;
  align-items: center;
  gap: 10px;
}

.ndsp-topbar__context {
  display: flex;
  flex-direction: column;
  gap: 2px;
}

.ndsp-topbar__eyebrow {
  color: var(--ndsp-text-muted);
  font-size: .68rem;
}

.ndsp-topbar__context strong {
  font-size: .9rem;
}

.ndsp-icon-button,
.ndsp-language-button {
  display: inline-flex;
  min-width: 40px;
  height: 40px;
  align-items: center;
  justify-content: center;
  gap: 7px;
  border: 1px solid var(--ndsp-border);
  border-radius: 10px;
  background: var(--ndsp-surface);
  color: var(--ndsp-text-secondary);
  cursor: pointer;
}

.ndsp-icon-button:hover,
.ndsp-language-button:hover {
  border-color: var(--ndsp-border-strong);
  color: var(--ndsp-text);
}

.ndsp-language-button {
  padding-inline: 11px;
  font-size: .75rem;
  font-weight: 700;
}

.ndsp-user-avatar {
  display: grid;
  width: 40px;
  height: 40px;
  place-items: center;
  border: 1px solid rgba(56,189,248,.25);
  border-radius: 50%;
  background: var(--ndsp-primary-soft);
  color: var(--ndsp-primary);
  font-weight: 800;
}

.ndsp-mobile-menu,
.ndsp-sidebar__close,
.ndsp-sidebar-backdrop,
.ndsp-mobile-nav {
  display: none;
}

@media (max-width: 980px) {
  .ndsp-app-shell__workspace {
    margin-inline-start: 0;
  }

  .ndsp-sidebar {
    width: min(86vw, 310px);

=== 6. CURRENT MAIN INTEGRATION ===
10:import { CompletedDecisionsPanelV37 } from "./components/CompletedDecisionsPanelV37";
11:import { ApplicationShell } from "./components/layout/ApplicationShell";
48:function App() {
63:  const quality = livePrice ? 84 : 41;
66:  return (
67:    <ApplicationShell>
68:      <div className="terminal">
90:        <section className="hero card">
91:          <div className="heroText">
115:        <section className="card assetCard">
133:        <section className="card quality">
136:            <span>{quality}</span>
141:        <section className="card decisionState">
150:        <section className="card engines">
169:        <CompletedDecisionsPanelV37 />
172:    </ApplicationShell>

=== 7. DATA HOOK CONTRACTS ===
--- FILE=/home/nawaf511/empire-core-new/frontend/user-portal-vite/src/hooks/useMarket.js ---
import { useEffect, useState, useCallback } from "react";

/*
  مهم:
  BASE_URL فارغ لأن Vite Proxy سيحوّل:
  /api/...  --->  https://api.ndsp.app/api/...
*/
const BASE_URL = "";

export function useMarket(symbol) {
  const [price, setPrice] = useState(null);
  const [scenario, setScenario] = useState(null);
  const [allPrices, setAllPrices] = useState([]);
  const [loading, setLoading] = useState(false);
  const [error, setError] = useState(null);

  const fetchJson = async (url) => {
    const controller = new AbortController();
    const timeout = setTimeout(() => controller.abort(), 8000);

    try {
      const res = await fetch(url, {
        signal: controller.signal,
        cache: "no-store",
        headers: {
          Accept: "application/json"
        }
      });

      if (!res.ok) {
        throw new Error(`HTTP_${res.status}`);
      }

      return await res.json();
    } finally {
      clearTimeout(timeout);
    }
  };

  const fetchPrice = useCallback(async () => {
    try {
      setLoading(true);
      setError(null);

      const normalizedSymbol = String(symbol || "").trim().toUpperCase();
      const url = `${BASE_URL}/api/market/prices?symbol=${encodeURIComponent(normalizedSymbol)}`;

      const data = await fetchJson(url);
      const rows = Array.isArray(data?.prices) ? data.prices : [];

      const current = rows.find((r) => {
        const rowSymbol = String(r?.symbol || "").trim().toUpperCase();
        return rowSymbol === normalizedSymbol;
      });

      setAllPrices(rows);
      setPrice(current || null);

      if (!current) {
        setError(`NO_MATCH_FOR_${normalizedSymbol}`);
      }
    } catch (err) {
      console.error("PRICE FETCH ERROR:", err);

      setPrice(null);
      setAllPrices([]);
      setError(err?.message || "PRICE_FETCH_ERROR");
    } finally {
      setLoading(false);
    }
  }, [symbol]);

  const fetchScenario = useCallback(async () => {
    try {
      const normalizedSymbol = String(symbol || "").trim().toUpperCase();
      const url = `${BASE_URL}/api/scenario/levels?symbol=${encodeURIComponent(normalizedSymbol)}`;

      const data = await fetchJson(url);
      setScenario(data || null);
    } catch (err) {
      console.warn("SCENARIO FETCH ERROR:", err?.message);
      setScenario(null);
    }
  }, [symbol]);

  useEffect(() => {
    fetchPrice();
    fetchScenario();

    const id = setInterval(() => {
      fetchPrice();
      fetchScenario();
    }, 30000);

    return () => clearInterval(id);
  }, [fetchPrice, fetchScenario]);

  const refresh = async () => {
    await Promise.all([
      fetchPrice(),
      fetchScenario()
    ]);
  };

  return {
    price,
    scenario,
    allPrices,
    loading,
    error,
    refresh
  };
}
--- FILE=/home/nawaf511/empire-core-new/frontend/user-portal-vite/src/hooks/useCanonicalDecision.js ---
import { useCallback, useEffect, useState } from "react";

const BASE_URL = "";
const MODES = ["speculative", "investment"];

async function fetchJson(url) {
  const controller = new AbortController();
  const timeout = setTimeout(() => controller.abort(), 15000);

  try {
    const response = await fetch(url, {
      signal: controller.signal,
      cache: "no-store",
      headers: { Accept: "application/json" }
    });

    if (!response.ok) {
      throw new Error(`HTTP_${response.status}`);
    }

    return await response.json();
  } finally {
    clearTimeout(timeout);
  }
}

export function useCanonicalDecision(symbol, timeframe = "weekly") {
  const [payloads, setPayloads] = useState({
    speculative: null,
    investment: null
  });
  const [loading, setLoading] = useState(false);
  const [error, setError] = useState(null);

  const refresh = useCallback(async () => {
    const normalizedSymbol = String(symbol || "").trim().toUpperCase();
    const normalizedTimeframe = String(timeframe || "weekly").trim().toLowerCase();

    setLoading(true);
    setError(null);

    try {
      const settled = await Promise.allSettled(
        MODES.map(async (mode) => {
          const endpoint = `${BASE_URL}/api/decision/canonical-live?symbol=${encodeURIComponent(normalizedSymbol)}&timeframe=${encodeURIComponent(normalizedTimeframe)}&mode=${mode}`;
          const payload = await fetchJson(endpoint);
          return [mode, payload];
        })
      );

      const next = {};
      const failures = [];

      settled.forEach((result, index) => {
        const mode = MODES[index];
        if (result.status === "fulfilled") {
          next[mode] = result.value[1];
        } else {
          failures.push(`${mode}:${result.reason?.message || "FETCH_FAILED"}`);
        }
      });

      setPayloads((current) => ({ ...current, ...next }));

      if (failures.length === MODES.length) {
        throw new Error(failures.join("|"));
      }

      if (failures.length) {
        setError(failures.join("|"));
      }
    } catch (err) {
      setError(err?.message || "CANONICAL_FETCH_ERROR");
    } finally {
      setLoading(false);
    }
  }, [symbol, timeframe]);

  useEffect(() => {
    refresh();
    const id = setInterval(refresh, 30000);
    return () => clearInterval(id);
  }, [refresh]);

  return {
    speculative: payloads.speculative,
    investment: payloads.investment,
    loading,
    error,
    refresh
  };
}

--- FILE=/home/nawaf511/empire-core-new/frontend/user-portal-vite/src/hooks/useCompletedDecisionsV37.js ---
import { useCallback, useEffect, useState } from "react";

const ENDPOINTS = {
  current: "/api/completed-decisions",
  history: "/api/completed-decisions/history?limit=100",
  integrity: "/api/completed-decisions/history/integrity"
};

async function fetchJson(url) {
  const controller = new AbortController();
  const timeout = setTimeout(() => controller.abort(), 20000);

  try {
    const response = await fetch(url, {
      signal: controller.signal,
      cache: "no-store",
      headers: { Accept: "application/json" }
    });

    if (!response.ok) {
      throw new Error(`${url}:HTTP_${response.status}`);
    }

    return await response.json();
  } finally {
    clearTimeout(timeout);
  }
}

export function useCompletedDecisionsV37() {
  const [current, setCurrent] = useState(null);
  const [history, setHistory] = useState(null);
  const [integrity, setIntegrity] = useState(null);
  const [loading, setLoading] = useState(true);
  const [error, setError] = useState(null);
  const [lastLoadedAt, setLastLoadedAt] = useState(null);

  const refresh = useCallback(async () => {
    setLoading(true);
    setError(null);

    const settled = await Promise.allSettled([
      fetchJson(ENDPOINTS.current),
      fetchJson(ENDPOINTS.history),
      fetchJson(ENDPOINTS.integrity)
    ]);

    const failures = [];

    if (settled[0].status === "fulfilled") {
      setCurrent(settled[0].value);
    } else {
      failures.push(settled[0].reason?.message || "CURRENT_FETCH_FAILED");
    }

    if (settled[1].status === "fulfilled") {
      setHistory(settled[1].value);
    } else {
      failures.push(settled[1].reason?.message || "HISTORY_FETCH_FAILED");
    }

    if (settled[2].status === "fulfilled") {
      setIntegrity(settled[2].value);
    } else {
      failures.push(settled[2].reason?.message || "INTEGRITY_FETCH_FAILED");
    }

    if (failures.length) {
      setError(failures.join(" | "));
    }

    setLastLoadedAt(new Date());
    setLoading(false);
  }, []);

  useEffect(() => {
    refresh();
    const id = setInterval(refresh, 60000);
    return () => clearInterval(id);
  }, [refresh]);

  return {
    current,
    history,
    integrity,
    loading,
    error,
    lastLoadedAt,
    refresh
  };
}


=== 8. PRODUCTION SAFETY ===
PRODUCTION_INDEX_SHA256=2a9dd08b3e4cfe78556d0ca85b0b4fdf153886aee1fb92ce22884b9df76aa1c3
PUBLIC_HTTP=200

=== FINAL ===
HOME_DASHBOARD_PRECHECK=PASS
APPLICATION_SHELL=/home/nawaf511/empire-core-new/frontend/user-portal-vite/src/components/layout/ApplicationShell.tsx
DATA_HOOKS_CAPTURED=PASS
UI_PRIMITIVES_CAPTURED=PASS
DESIGN_SYSTEM_CAPTURED=PASS
PRODUCTION_MODIFIED=NO
DEPLOY_EXECUTED=NO
BUILD_EXECUTED=NO
NGINX_MODIFIED=NO
SERVICES_RESTARTED=NO
NEXT_ACTION=BUILD_HOME_DASHBOARD_V1
FINAL_RESULT=PASS_READ_ONLY
AUDIT=/home/nawaf511/empire-core-new/var/audits/home-dashboard-precheck-v1-20260808-060754
REPORT=/home/nawaf511/empire-core-new/var/audits/home-dashboard-precheck-v1-20260808-060754/report.txt
