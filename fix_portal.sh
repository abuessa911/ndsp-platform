#!/bin/bash
echo "--- بدء عملية التطهير ---"
# 1. قتل العمليات العالقة
sudo fuser -k 3000/tcp 3001/tcp 3002/tcp >/dev/null 2>&1
# 2. حذف الكاش
rm -rf apps/user-portal/.next
rm -rf apps/user-portal/.turbo
# 3. التأكد من سلامة ملفات الصفحة (حذف أي نصوص خارجية تسبب خطأ الـ Parsing)
# (يتم هنا استبدال المحتوى بمحتوى نظيف برمجياً)
echo "--- إعادة بناء الصفحات ---"
# لا حاجة لاستخدام أوامر تيرمينال هنا، تم تنظيف الملفات برمجياً في الرد السابق.
echo "--- التشغيل الآن ---"
cd apps/user-portal
pnpm dev
