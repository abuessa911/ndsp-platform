name: PR-048 P0 Batch 10 Evidence Closure

on:
  pull_request:
    branches: [main]
  push:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: read

jobs:
  validate-p0-batch-10-evidence-closure:
    runs-on: ubuntu-latest
    timeout-minutes: 10

    steps:
      - uses: actions/checkout@v5
        with:
          fetch-depth: 0

      - name: Fetch base branch
        run: git fetch --no-tags origin main:refs/remotes/origin/main

      - uses: actions/setup-node@v5
        with:
          node-version: "22"

      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Run PR-048 tests
        run: node --test scripts/governance/tests/pr048-p0-batch-10-evidence-closure.test.cjs

      - name: Validate PR-048 closure
        run: python3 scripts/governance/validate_pr048_p0_batch_10_evidence_closure.py

      - name: Validate capability governance
        run: |
          python3 scripts/governance/validate_full_capability_ui.py \
            --base-ref origin/main
